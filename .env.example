# ==============================================================================
# MC Coin Bot — Environment Variables
# ==============================================================================
# Coolify: UI에서 이 변수들을 설정하세요.
# Local: 이 파일을 .env로 복사 후 값을 채우세요.
# ==============================================================================

# === Binance API (필수) ===
BINANCE_API_KEY=
BINANCE_SECRET=

# === Execution (필수) ===
MC_EXECUTION_MODE=paper              # paper | shadow | live
MC_CONFIG_PATH=config/paper.yaml     # YAML 설정 파일 경로
MC_INITIAL_CAPITAL=10000.0           # 초기 자본 (USD)

# === Database ===
MC_DB_PATH=data/trading.db           # SQLite 경로 (빈 문자열 = 비활성)
MC_ENABLE_PERSISTENCE=true           # 상태 영속화 on/off

# === Logging ===
LOG_DIR=logs                         # 로그 디렉토리
LOG_CONSOLE_LEVEL=INFO               # DEBUG | INFO | WARNING | ERROR
LOG_FILE_LEVEL=DEBUG                 # 파일 로그 레벨
LOG_JSON_LOGS=true                   # JSON 포맷 로깅

# === Discord Notifications (선택) ===
# DISCORD_BOT_TOKEN=
# DISCORD_GUILD_ID=
# DISCORD_TRADE_LOG_CHANNEL_ID=
# DISCORD_ALERTS_CHANNEL_ID=
# DISCORD_DAILY_REPORT_CHANNEL_ID=
# DISCORD_HEARTBEAT_CHANNEL_ID=
# DISCORD_REGIME_CHANNEL_ID=

# === Data Directories (선택 — 기본값 있음) ===
# BRONZE_DIR=data/bronze
# SILVER_DIR=data/silver
