# =============================================================================
# MC Coin Bot — Environment Variables
# =============================================================================
# 이 파일을 .env 로 복사한 후 실제 값을 입력하세요.
#   cp .env.example .env
#
# 주의: .env 파일은 절대 git에 커밋하지 마세요 (.gitignore에 포함됨)
# =============================================================================

# =============================================================================
# Binance API (필수 — 데이터 수집 및 트레이딩)
# =============================================================================
# 발급: https://www.binance.com/en/my/settings/api-management
#
# 보안 체크리스트:
#   - Spot & Margin Trading: 활성화
#   - Futures: 필요 시에만 활성화
#   - Withdrawals: 절대 비활성화 유지
#   - IP 제한: VPS 고정 IP만 허용 (권장)
#   - 데이터 조회용 / 거래용 API key 분리 (권장)
#
BINANCE_API_KEY=
BINANCE_SECRET=

# =============================================================================
# Discord Bot (필수 — 알림 + 양방향 명령)
# =============================================================================
# 설정 순서:
#   1. https://discord.com/developers/applications → New Application
#   2. Bot 메뉴 → Reset Token → 토큰 복사
#   3. Bot 설정: Public Bot = OFF, Message Content Intent = OFF
#   4. OAuth2 → URL Generator → scope: bot + applications.commands
#      permissions: Send Messages, Embed Links, Attach Files, Mention Everyone
#   5. 생성된 URL로 서버에 봇 초대
#   6. 개발자 모드 ON (설정 → 고급) → 채널/서버 우클릭 → ID 복사
#
DISCORD_BOT_TOKEN=
DISCORD_GUILD_ID=

# 채널 ID (서버에 채널 생성 후 ID 복사)
#   #trade-log:     거래 체결 기록 (INFO)
#   #alerts:        긴급 알림, SL/TS/CB (WARNING+)
#   #daily-report:  일일/주간 리포트 + 차트
DISCORD_TRADE_LOG_CHANNEL_ID=
DISCORD_ALERTS_CHANNEL_ID=
DISCORD_DAILY_REPORT_CHANNEL_ID=

# =============================================================================
# Discord Webhook (선택 — 로깅 에러 알림 전용)
# =============================================================================
# 기존 로깅 시스템(src/logging/sinks/discord.py)에서 사용
# Discord Bot과 별개로 동작
#
# DISCORD_TRADE_WEBHOOK_URL=
# DISCORD_ERROR_WEBHOOK_URL=
# DISCORD_REPORT_WEBHOOK_URL=

# =============================================================================
# Data Directories (선택 — 기본값 있음)
# =============================================================================
# BRONZE_DIR=data/bronze
# SILVER_DIR=data/silver
# LOG_DIR=logs

# =============================================================================
# Logging (선택 — 기본값 있음)
# =============================================================================
# LOG_CONSOLE_LEVEL=INFO
# LOG_FILE_LEVEL=DEBUG
# LOG_ROTATION=50 MB
# LOG_RETENTION=7 days
# LOG_JSON_LOGS=true
# LOG_ENABLE_DISCORD=false
# LOG_DISCORD_WEBHOOK_URL=
# LOG_DISCORD_MIN_LEVEL=ERROR
