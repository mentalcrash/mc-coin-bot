---
description: Git Conventions & Security Hygiene (Conventional Commits)
globs: .gitignore, .git/**, **/*.md
alwaysApply: true
---

# 🛡️ Git & Security Hygiene

## 1. Security First (The Firewall)
- **No Secrets in Git:** `.env`, `*.pem`, `*.key`, `secrets.json` 등 민감 파일은 **반드시** `.gitignore`에 포함되어야 합니다.
- **Pre-commit Check:** 커밋 전, 코드 내에 하드코딩된 API Key(sk-...)가 없는지 다시 한번 확인합니다. (Rule `S101` 연동)

## 2. Conventional Commits
- **Format:** `type(scope): description` 형식을 엄격히 준수합니다.
    - `feat`: 새로운 기능 (예: `feat(strategy): add rsi indicator`)
    - `fix`: 버그 수정 (예: `fix(ccxt): resolve precision error`)
    - `docs`: 문서 수정
    - `style`: 코드 포맷팅 (로직 변경 없음)
    - `refactor`: 코드 리팩토링
    - `test`: 테스트 코드 추가
    - `chore`: 빌드 설정, 패키지 매니저 설정 등

## 3. Branching Strategy (Trunk Based)
- **Feature Branches:** 모든 작업은 `main` 브랜치가 아닌 `feat/...` 또는 `fix/...` 브랜치에서 수행하는 것을 권장합니다.

## 4. Documentation Maintenance
- **README Update:** 새로운 모듈이나 환경 변수가 추가되면 `README.md`의 'Setup' 섹션을 반드시 업데이트합니다.