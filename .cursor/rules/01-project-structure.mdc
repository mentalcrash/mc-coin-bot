---
description: Project Directory Structure & Module Responsibilities (Src Layout)
globs: *
alwaysApply: true
---

# 🏗️ Project Structure & Module Responsibilities

## 1. Root Layout Standard (`src` Pattern)
- **Source Root:** 모든 소스 코드는 반드시 `src/` 디렉토리 하위에 위치해야 합니다.
- **Top-level:** 루트(`/`)에는 `pyproject.toml`, `README.md`, `.env`, `AGENTS.md` 등 설정 및 문서 파일만 위치합니다.

## 2. Directory Map & Rules Mapping
AI는 코드를 생성하거나 수정할 때, 아래의 디렉토리별 책임과 적용되는 룰(Rules)을 준수해야 합니다.

### 🔹 Core & Configuration
- **Path:** `src/core/`
- **Responsibility:** 로깅 설정, 상수 정의, 환경 변수 로드.
- **Applied Rules:** `11-pydantic-modeling.mdc` (설정 관리)

### 🔹 Data Layer (ETL)
- **Path:** `src/data/`
- **Responsibility:** 데이터 수집, 저장(Parquet), 로딩(DuckDB).
- **Applied Rules:** `12-data-engineering.mdc` (Vectorization, No Loops)
- **Structure:**
    - `loader/`: 외부 소스에서 데이터 fetch (API 호출 제외, 순수 데이터 처리)
    - `storage/`: DuckDB/Parquet 관리

### 🔹 Exchange Layer (Connectivity)
- **Path:** `src/exchange/`
- **Responsibility:** CCXT Pro 연결, WebSocket 관리, 정밀도 보정.
- **Applied Rules:** `14-ccxt-trading.mdc` (Async, Precision, WebSocket)
- **Constraint:** 이곳에는 매매 전략(Strategy) 로직이 포함되어서는 안 됩니다. 오직 "연결"과 "주문 전송"만 담당합니다.

### 🔹 Domain Models (DTO)
- **Path:** `src/models/`
- **Responsibility:** 데이터 구조 정의, 유효성 검사.
- **Applied Rules:** `11-pydantic-modeling.mdc` (Strict Typing, Frozen)

### 🔹 Strategy Layer (Brain)
- **Path:** `src/strategy/`
- **Responsibility:** 시그널 생성, 지표 계산(Pandas-TA), 매매 판단.
- **Applied Rules:** `12-data-engineering.mdc` (Vectorized Indicators)

### 🔹 Execution Layer (Action)
- **Path:** `src/execution/`
- **Responsibility:** 주문 집행, 리스크 관리(Kill Switch), 포지션 추적.
- **Applied Rules:** `20-crypto-safety.mdc` (Safety Rails, Double Check)

### 🔹 Backtesting & Research
- **Path:** `src/backtest/` (또는 `research/`)
- **Responsibility:** VectorBT 시뮬레이션, 전략 최적화.
- **Applied Rules:** `12-data-engineering.mdc` (VBT Broadcasting)

## 3. Dependency Flow (Unidirectional)
순환 참조(Circular Dependency)를 방지하기 위해 아래의 의존성 방향을 엄격히 지킵니다.
- ✅ `Strategy` -> imports -> `Data`, `Models`
- ✅ `Execution` -> imports -> `Exchange`, `Models`
- ✅ `Main` -> imports -> `Strategy`, `Execution`
- ❌ `Models` -> imports -> `Strategy` (Forbidden)

## 4. File Naming Convention
- **Modules:** `snake_case.py` (예: `market_data.py`, `risk_manager.py`)
- **Classes:** `PascalCase` (예: `MarketDataManager`, `RiskController`)
- **Tests:** `tests/` 폴더 내에 `src` 구조와 동일하게 미러링하며, 파일명 접두사는 `test_`로 시작합니다.